# Terraform state (contains live infra details — never commit)
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform/

# Built artifacts (regenerated by build.sh)
terraform/lambda_failover.zip
lambda/package/
lambda/lambda_failover.zip

# Python
__pycache__/
*.pyc

# Blog post (published separately)
BLOG.md

# macOS
.DS_Store
